%h1 Discussion Forum

#post
    - @posts.each do |post|
        .post   
        %h2= link_to post.title, post
        %p
        Published 
        = time_ago_in_words(post.created_at)
        = "ago by"
        = post.user.email